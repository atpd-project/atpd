# ATP Versions Configuration

# musl toolchain
MUSL_TOOLCHAIN_VERSION=20250929
MUSL_TOOLCHAIN_URL=https://github.com/cross-tools/musl-cross/releases/download/${MUSL_TOOLCHAIN_VERSION}/aarch64-unknown-linux-musl.tar.xz
MUSL_TOOLCHAIN_TRIPLET=aarch64-unknown-linux-musl

# libev (replaces libcurl)
LIBEV_VERSION=4.33
LIBEV_DOWNLOAD_URL=http://dist.schmorp.de/libev/libev-${LIBEV_VERSION}.tar.gz

# ATP
ATP_VERSION=1.0.0
