name: Build ATP for Android (musl)

run-name: "#${{ github.run_number }}: ${{ github.event.pull_request.title || github.event.head_commit.message }}"

on:
  push:
    branches:
      - main
      - dev
      - staging
      - 'feat-*'
      - 'feat/**'
      - 'test/**'
    paths:
      - 'src/**'
      - 'include/**'
      - 'scripts/**'
      - 'Makefile'
      - 'Dockerfile'
      - '.github/workflows/build.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-verify:
    runs-on: ubuntu-latest
    outputs:
      binary_size: ${{ steps.extract.outputs.binary_size }}
      atp_version: ${{ steps.extract.outputs.atp_version }}
      build_type: ${{ steps.build.outputs.build_type }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v4

      - name: Determine Build Type
        id: build_type
        run: |
          BRANCH="${{ github.ref_name }}"
          if [[ "$BRANCH" == "staging" ]] || [[ "$BRANCH" == feat/* ]] || [[ "$BRANCH" == test/* ]]; then
            echo "type=debug" >> $GITHUB_OUTPUT
            echo "flags=-O0 -g -fsanitize=address -DATP_DEBUG" >> $GITHUB_OUTPUT
          else
            echo "type=release" >> $GITHUB_OUTPUT
            echo "flags=-Os -flto -fstack-protector-strong -D_FORTIFY_SOURCE=3 -ffunction-sections -fdata-sections" >> $GITHUB_OUTPUT
          fi
          echo "Build type: ${{ steps.build_type.outputs.type }}"

      - name: Build Docker Image
        id: build
        run: |
          docker buildx build --platform linux/arm64 \
            --build-arg BUILD_FLAGS="${{ steps.build_type.outputs.flags }}" \
            --target builder -t atp-builder --load .
          container_id=$(docker create --platform linux/arm64 atp-builder)
          docker cp $container_id:/app/build/bin/atpd ./atpd
          docker rm -v $container_id
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Verify Binary
        run: |
          if [ ! -s "./atpd" ]; then
            echo "::error::Binary not found or is empty"
            exit 1
          fi
          file ./atpd
          ls -lh ./atpd

      - name: Extract Metadata
        id: extract
        run: |
          SIZE=$(du -h ./atpd | cut -f1)
          echo "binary_size=$SIZE" >> $GITHUB_OUTPUT
          VER="v0.0.1-$(git rev-parse --short HEAD)"
          echo "atp_version=$VER" >> $GITHUB_OUTPUT

      - name: Upload Artifact
        uses: actions/upload-artifact@v5
        with:
          name: atpd-${{ steps.build_type.outputs.type }}-${{ github.sha }}
          path: ./atpd
          retention-days: 7

  update-status:
    name: Update Checkpoint
    needs: build-and-verify
    if: success()
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Update Project Issue
        uses: actions/github-script@v7
        env:
          SIZE: ${{ needs.build-and-verify.outputs.binary_size }}
          VER: ${{ needs.build-and-verify.outputs.atp_version }}
          BUILD_TYPE: ${{ needs.build-and-verify.outputs.build_type }}
          TARGET_ISSUE: 6
          COMMIT_MSG: ${{ github.event.head_commit.message }}
        with:
          script: |
            const updateScript = require('./.github/scripts/update_issue.js');
            await updateScript({ github, context, core });

      - name: Notify Telegram
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GH_TOKEN: ${{ github.token }}
          BUILD_TYPE: ${{ needs.build-and-verify.outputs.build_type }}
        run: |
          chmod +x .github/scripts/notify_issue6.sh
          ./.github/scripts/notify_issue6.sh
